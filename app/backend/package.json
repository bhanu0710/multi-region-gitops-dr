{
  "name": "dr-demo-backend",
  "version": "1.0.0",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "echo 'No tests yet — add Jest tests before production use' && exit 0"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
